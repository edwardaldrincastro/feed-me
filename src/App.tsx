import * as React from "react";
import { Text, View } from "react-native";

interface AppProps {}

const App = (props: AppProps) => {
  return (
    <View>
      <Text>App</Text>
    </View>
  );
};

export default App;
